For this repo to run, it requires that you have a postgres instance and a MySQL server with some data in it that matches the defined credentials. 
You also need to create a dotenv file for your connection strings.